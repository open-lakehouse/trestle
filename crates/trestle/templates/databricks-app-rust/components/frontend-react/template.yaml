name: frontend-react
version: 0.1.0
description: |
  React + Vite + TypeScript frontend. The generated TS client (from trestle
  generate) lands in `frontend/src/api/`; this component sets up package.json,
  vite.config.ts, tsconfig.json, and a sample component that calls the
  generated client.

provides: {}
