// Placeholder until `trestle generate` runs. The generator overwrites this file
// with `pub mod <service>;` declarations (one per proto service). Declared as
// `mod gen;` from `main.rs` so the output directory always exists for codegen.
