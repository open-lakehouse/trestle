//! Shared types for the {{ project_name }} app. The `models` module is populated
//! by `trestle generate` from `proto/{{ project_name | snake_case }}/v1/*.proto`.

pub mod models {
    //! Generated model types (proto types generated by buf under `_gen/`).
    //! Re-exported here so consumers import from a stable path.
    include!("models/mod.rs");
}
