// Placeholder until `just regen` runs. `buf generate` overwrites this directory
// with the ConnectRPC service facade (via the remote `connect-rust` plugin +
// the local `protoc-gen-buffa-packaging`). Mounted as `mod connect_gen;` from
// `main.rs`, so the directory must exist for the first `cargo build`.
