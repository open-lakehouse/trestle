# Use legacy peer-dependency resolution: npm's strict peer solver (npm 7+) can
# fail an otherwise-valid install when a registry/proxy hiccup leaves a peer
# (e.g. `vite`) momentarily unresolved. The version ranges here are compatible;
# this just keeps `npm install` from hard-failing on transient peer-resolution.
legacy-peer-deps=true
